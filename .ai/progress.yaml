version: 3
last_updated: 2026-05-28
current_phase: phase_3_user_journey

phases:
  phase_1_core_search:
    status: done
    backend_tasks:
      - id: setup_docker
        status: done
      - id: design_database
        status: done
      - id: implement_parking_entity
        status: done
      - id: nearby_search_api
        status: done
      - id: backend_api_fields_fix
        status: done
        note: "Added totalSlots, hourlyRate, parkingType, amenity flags to SQL query + DTO pipeline"
    frontend_tasks:
      - id: setup_angular
        status: done
      - id: map_view_basic
        status: done
      - id: call_nearby_api
        status: done
      - id: render_parking_markers
        status: done
      - id: style_architecture
        status: done
        note: "Tailwind v4 @theme, partials (base/components/utilities), angular.json updated"
      - id: icon_system
        status: done
        note: "@lucide/angular, IconComponent, 24-icon registry, replaces all SVGs"
      - id: marker_transform_fix
        status: done
        note: "Wrapper+child DivIcon model -- Leaflet translate3d separated from CSS scale/rotate"
      - id: parking_card_redesign
        status: done
        note: "Vertical layout: title, meta row (distance+price), status row with dot"

  phase_2_booking:
    status: done
    backend_tasks:
      - id: implement_booking_naive
        status: done
      - id: reproduce_race_condition
        status: done
      - id: implement_redis_atomic_booking
        status: done
        note: "Booking now uses Redis Lua atomic reservation before DB insert and rolls back Redis on DB save failure"
      - id: flexible_booking_duration
        status: done
        note: "Accept duration param (1h/2h/4h/daily) instead of hardcoded 1-hour window"
      - id: booking_status_lifecycle
        status: done
        note: "PENDING/ACTIVE/COMPLETED/CANCELLED/EXPIRED states; BookingLifecycleScheduler (@Scheduled 60s) drives transitions; PATCH /bookings/{id}/cancel endpoint; Redis slot released on terminal transitions"
    frontend_tasks:
      - id: parking_detail_page
        status: done
        note: "Detail panel component complete and wired to main map booking flow"
      - id: detail_panel_sticky_footer
        status: done
        note: "3-zone layout: pinned header, scrollable body, pinned actions footer with 2 CTA buttons"
      - id: detail_panel_amenities
        status: done
        note: "9 parkingType-keyed amenity sets (2-6 items), data-driven *ngFor, dynamic aboutText"
      - id: detail_panel_image_section
        status: done
        note: "Media promoted to pinned zone, clamp(180px,28vh,260px) height, opacity-fade carousel, gradient overlay, zoom-on-hover"
      - id: zoom_control_repositioning
        status: done
        note: "Leaflet bottomleft position, responsive offsets relative to sidebar"
      - id: rich_seed_data
        status: done
        note: "V3 migration: 10 diverse HCMC parkings, hourlyRate/parkingType/amenity flag columns"
      - id: booking_button
        status: done
        note: "Reserve Spot in detail panel creates demo-user one-hour booking via POST /bookings"
      - id: booking_result_feedback
        status: done
        note: "Map shell shows booking success, no-slot, and reservation-unavailable feedback"
      - id: booking_duration_selector
        status: done
        note: "UI selector (1h/2h/4h/daily) in detail panel before Reserve button"
      - id: search_filter_basic
        status: done
        note: "Wire search bar to filter parking list by name/address"

  phase_3_user_journey:
    status: in_progress
    note: "Auth + booking lifecycle — must come BEFORE realtime (WS needs identity)"
    backend_tasks:
      - id: basic_auth_jwt
        status: done
        note: "Spring Security + JWT access/refresh flow, auth endpoints, secured /bookings, booking userId sourced from JWT principal"
      - id: current_user_endpoint
        status: done
        note: "GET /users/me implemented with JWT principal resolution and secured route"
      - id: booking_history_api
        status: done
        note: "GET /bookings with page/size/sort/status supports Spring Page response scoped to authenticated principal"
      - id: swagger_api_docs_standardization
        status: done
        note: "Springdoc Swagger UI enabled, OpenAPI bearer scheme configured, and endpoint tag/summary/description/response annotations standardized"
      - id: active_booking_api
        status: done
        note: "GET /bookings/active returns current user's ACTIVE booking; empty result returns HTTP 200 with empty body"
      - id: booking_detail_api
        status: done
        note: "GET /bookings/{bookingId} returns enriched BookingDetailResponse (parkingName, address) scoped to authenticated principal; 404 for both not-found and wrong-owner"
      - id: booking_ownership_hardening
        status: pending
        note: "Enforce ownership checks on booking by-id and cancel endpoints to prevent cross-user access"

    frontend_tasks:
      - id: sign_in_page
        status: done
        note: "Map-persistent auth overlay implemented; /auth/sign-in kept as compatibility redirect to /map with modal intent"
      - id: sign_up_page
        status: done
        note: "Sign-up mode added inside auth overlay with /auth/register integration and switch-back-to-sign-in flow"
      - id: auth_state_management
        status: done
        note: "HTTP interceptor attaches Bearer token, authGuard protects booking routes, AuthSessionService holds access token, withInterceptors wired in main.ts bootstrap"
      - id: current_booking_view
        status: pending
        note: "Active booking banner showing parking name + remaining time"
      - id: booking_history_view
        status: pending
        note: "List of past bookings with status badges"
      - id: map_shell_nested_routing
        status: done
        note: "Added /map, /map/:parkingId, /map/:parkingId/booking routes with persistent shell and route-driven state"
      - id: booking_overlay_route
        status: done
        note: "Booking confirmation overlay now appears on /map/:parkingId/booking and keeps map mounted"
      - id: auth_return_to_intent
        status: done
        note: "Auth modal and /auth/* redirect bridge preserve returnUrl; booking intent resumed after sign-in via route state"
      - id: mobile_desktop_overlay_parity
        status: pending
        note: "Same route/state, different presentation: desktop right drawer, mobile bottom sheet"

  phase_4_realtime:
    status: pending
    note: "Meaningful only after user identity + booking state exist"
    backend_tasks:
      - id: websocket_setup
        status: pending
      - id: redis_pubsub
        status: pending
      - id: simulate_occupancy
        status: pending
        note: "Admin endpoint to +1/-1 car for live demo — triggers realtime push"
    frontend_tasks:
      - id: websocket_client
        status: pending
      - id: realtime_slot_update_ui
        status: pending
      - id: availability_timestamp
        status: pending
        note: "Show 'Updated Xs ago' on parking cards for trust signal"

  phase_5_ux_and_integrations:
    status: pending
    backend_tasks:
      - id: cache_optimization
        status: pending
      - id: error_contract_hardening
        status: pending
      - id: google_oauth2
        status: pending
        note: "OAuth2 with Google as login option — after basic auth is stable"
    frontend_tasks:
      - id: loading_state
        status: pending
      - id: error_state
        status: pending
      - id: current_position_marker
        status: pending
        note: "Geolocation API + pulsing blue dot marker for user location"
      - id: directions_deeplink
        status: pending
        note: "Google Maps deep-link with proper origin/destination params"
      - id: highlight_almost_full
        status: pending
      - id: filter_chips
        status: pending
        note: "EV charging, roofed, price range — data already in V3 seed"
      - id: active_booking_banner
        status: pending
        note: "Persistent banner: 'You have a reservation at X — 42 min remaining'"

notes:
  - "Phase reorder: Auth before Realtime — WS connections need user identity."
  - "Flexible booking duration is core booking flow, NOT UX polish — stays in phase 2."
  - "Booking status lifecycle (PENDING/ACTIVE/COMPLETED/CANCELLED/EXPIRED) is portfolio gold for backend thinking."
  - "simulate_occupancy in phase 4 enables powerful live demo without real IoT hardware."
  - "OAuth2 Google deferred to phase 5 — basic JWT auth ships faster and shows more engineering depth."
  - "Payment integration intentionally excluded — high effort, low portfolio ROI."
  - "Race condition was reproduced via Postman and a basic script with 10 simultaneous booking requests."
  - "Redis atomic booking gate is enabled with fail-closed behavior when Redis is unavailable."
